# **คู่มือพัฒนาระบบรายงานประจำวันอัตโนมัติ (Daily Report Automation System Blueprint)**

*เอกสารพิมพ์เขียวระบบฉบับสมบูรณ์ (เวอร์ชันล่าสุด) สำหรับส่งต่อให้ AI หรือ Gemini CLI พัฒนาเป็น Web Application รันบนภาษา Python (Streamlit \+ PostgreSQL \+ Playwright \+ Gemini AI)*

## **1\. ภาพรวมและสถาปัตยกรรมระบบ (System Overview)**

ระบบนี้ถูกออกแบบมาเพื่อลดเวลาในงานประจำวัน (Routine Task) ของเจ้าหน้าที่ในการจัดทำรายงานรูปภาพและข่าวสารประจำวัน โดยผู้ใช้สามารถล็อกอินเข้าระบบผ่านโทรศัพท์มือถือ เพื่ออัปโหลดรูปภาพกิจกรรม ป้อนข้อความ และระบุคำสำคัญเพื่อให้ AI (Gemini API) เจนบทความไทยที่ถูกต้องตามหลักภาษาให้อัตโนมัติ

ระบบมีจุดเด่นคือ**ความยืดหยุ่นสูงแบบรายบุคคล (Multi-user Settings)** ที่เปิดให้พนักงานแต่ละคนสามารถเข้ามากำหนด API Key ของ Gemini และ Token/Chat ID ของ Telegram Bot ของตนเองได้ เพื่อให้ระบบส่งภาพผลลัพธ์ PNG ขนาด 800x1150px ตรงไปยังกลุ่มรายงานของแผนกตนเองทันที โดยยังมีการออกแบบการปรับขนาดตัวหนังสือและฟอนต์ (Font Family) ได้อย่างอิสระบนสไลเดอร์มือถือ เพื่อไม่ให้สระหรือข้อความล้นทะลักออกจากกรอบดีไซน์สไลด์

### **เทคโนโลยีที่เลือกใช้ (Tech Stack)**

* **Web UI Framework:** Streamlit (ใช้งานง่าย สะดวกสำหรับการทำ Responsive UI บนมือถือ)  
* **Database:** PostgreSQL (มีความเสถียร รองรับการจัดเก็บพาธรูปภาพหลายรูปในฟิลด์แบบ JSONB)  
* **AI Engine:** Google Gemini API (ใช้วิเคราะห์ประเด็นและแต่งบทความภาษาไทยเป็นทางการ)  
* **Image Engine:** Jinja2 ร่วมกับ Playwright (เบราว์เซอร์จำลองแบบ Headless เพื่อแปลงโครงสร้าง HTML/CSS เป็นไฟล์ภาพ PNG ทรงแนวตั้ง)  
* **Notification:** Telegram Bot API (สำหรับยิงส่งภาพเข้ากลุ่มสนทนาแบบอัตโนมัติ)  
* **Authentication:** streamlit-authenticator (จัดการการเข้าสู่ระบบและจำสิทธิ์ Cookie)

## **2\. โครงสร้างฐานข้อมูล (Database Schema \- PostgreSQL)**

ระบบเลือกใช้ PostgreSQL และใช้ฟีเจอร์ JSONB ในการจัดการพาธรูปภาพกิจกรรมแบบไดนามิก พร้อมเชื่อมต่อแบบ One-to-One และ One-to-Many สำหรับการใช้งานแบบหลายผู้ใช้

### **ตารางผู้ใช้งาน (users)**

| Field Name | Data Type | Constraints | Description   |
| :---- | :---- | :---- | :---- |
| **id** | SERIAL | PRIMARY KEY | รหัสอ้างอิงผู้ใช้งาน |
| **username** | VARCHAR(50) | UNIQUE NOT NULL | ชื่อบัญชีเข้าใช้งาน |
| **password\_hash** | VARCHAR(255) | NOT NULL | รหัสผ่านที่เข้ารหัสด้วย bcrypt |
| **full\_name** | VARCHAR(100) | \- | ชื่อ-นามสกุลจริงผู้ใช้งาน |
| **role** | VARCHAR(20) | DEFAULT 'staff' | บทบาทการใช้งาน (admin, staff) |

### **ตารางตั้งค่าส่วนตัวผู้ใช้ (user\_settings)**

| Field Name | Data Type | Constraints | Description   |
| :---- | :---- | :---- | :---- |
| **user\_id** | INT | PRIMARY KEY, FK(users) | รหัสผู้ใช้เชื่อมตารางหลัก |
| **gemini\_api\_key** | VARCHAR(255) | NULL | คีย์ประมวลผล AI ของส่วนตัวผู้ใช้ |
| **telegram\_bot\_token** | VARCHAR(255) | NULL | บอทแชต Telegram ของตนเอง |
| **telegram\_chat\_id** | VARCHAR(100) | NULL | ไอดีกลุ่มปลายทางส่งงาน |
| **ai\_system\_instruction** | TEXT | NULL | คำสั่งควบคุมลักษณะเขียนบทความเฉพาะตัว |

### **ตารางรายงานประจำวัน (daily\_reports)**

| Field Name | Data Type | Constraints | Description   |
| :---- | :---- | :---- | :---- |
| **id** | SERIAL | PRIMARY KEY | รหัสเอกสารรายงาน |
| **user\_id** | INT | FK(users.id) | เจ้าหน้าที่ผู้ทำรายการ |
| **report\_date** | DATE | NOT NULL | วันที่บันทึกรายงาน |
| **header\_text** | VARCHAR(255) | DEFAULT 'ตม.จว.นครศรีธรรมราช' | ชื่อหน่วยงานป้ายเขียวส่วนหัว |
| **title\_text** | VARCHAR(255) | NOT NULL | ข้อความหัวเรื่องหลักของกิจกรรม |
| **content** | TEXT | \- | บทความสรุปเหตุการณ์ (AI หรือพิมพ์เอง) |
| **image\_paths** | JSONB | NULL | พาธรูปกิจกรรม บันทึกเป็นอาเรย์ JSON |
| **font\_family** | VARCHAR(100) | DEFAULT 'TH Sarabun IT 9' | สไตล์ตัวอักษรของรายงาน |
| **content\_font\_size** | INT | DEFAULT 19 | ขนาดฟอนต์ของช่องเนื้อหาบทความ (px) |

## **3\. โครงสร้างโฟลเดอร์โปรเจกต์ (Project Structure)**

การจัดระบบไฟล์ของ Python และเทมเพลตสำหรับขึ้นงานมีโครงสร้างและวัตถุประสงค์ดังนี้:

daily-report-app/  
├── app.py                  \# ไฟล์หลัก Streamlit UI สำหรับกรอกข้อมูลบนหน้าจอมือถือ  
├── database.py             \# ฟังก์ชัน CRUD สำหรับบันทึก ค้นหา และดึงประวัติ PostgreSQL  
├── gemini\_ai.py            \# สคริปต์เชื่อมโยง Gemini API และคำนวณโควตา  
├── image\_generator.py      \# ผนวกตัวแปรและรูปกิจกรรมเข้า Template สั่งเรนเดอร์ภาพด้วย Playwright  
├── telegram\_bot.py         \# ฟังก์ชันส่งข้อความและแชร์ภาพ PNG เข้ากลุ่ม Telegram ของยูสเซอร์  
├── config.yaml             \# ไฟล์สำหรับ Streamlit Authenticator คอนฟิกบัญชีผู้ใช้  
├── .env                    \# เก็บ API Key และคอนฟิกของ PostgreSQL ส่วนกลาง (Global Config)  
└── templates/  
    ├── report\_template.html  \# เทมเพลตโครงร่างสไลด์ (สไลด์แนวตั้งคงที่ 800x1150px)  
    ├── default\_header.png    \# รูป Header พื้นหลังสำเร็จรูปที่มีโลโก้สำนักงาน  
    └── default\_footer.png    \# รูป Footer ลายน้ำแนวนโยบาย "4 ส." สำเร็จรูป

## **4\. ความต้องการเชิงฟังก์ชัน (Functional Requirements)**

ระบบการทำงานจะถูกแบ่งออกเป็น 3 เมนูหลักบน Streamlit Web Interface ซึ่งได้รับการออกแบบ Layout ให้เหมาะสมกับการเปิดใช้งานบนมือถือเป็นลำดับแรก:

1. **หน้าทำรายการใหม่ (Create Report):** สำหรับป้อนข้อมูลหัวข้อหลัก อัปโหลดภาพถ่าย 2 ภาพ พร้อมชุด Sliders ปรับขนาดอักษร 3 จุดแบบเรียลไทม์ และปุ่มเรียก AI ประมวลเนื้อหา ตรวจทานเสร็จสามารถกดส่งตรงเข้ากล่องแชตของ Telegram ได้ทันที  
2. **หน้าประวัติและแก้ไขรายงาน (Search & Edit):** ระบบจัดการดึงรายงานเก่าตามสิทธิ์ (Staff เห็นเฉพาะของตนเอง, Admin เห็นของทุกคน) เมื่อกดเลือกรายงานเก่า ระบบจะดึงภาพพร้อมพารามิเตอร์ขนาดอักษรและประเภทฟอนต์ดั้งเดิมมาตั้งต้นฟอร์มเพื่อให้แก้ไขได้สะดวกรวดเร็ว  
3. **หน้าการตั้งค่าส่วนบุคคล (User Settings):** สำหรับให้ผู้ใช้แต่ละคนกรอก API Key ของตนเองและ Telegram Chat ID เพื่อความสะดวกในการแยกส่งรูปรายงานเข้าห้องกลุ่มแชตของแผนกโดยตรง

## **5\. พรอมต์สั่งงานหลักในการเขียนโค้ดร่วมกับ AI (Development Prompts)**

คุณสามารถคัดลอกพรอมต์เหล่านี้ไปใช้ในกระบวนการเขียนโปรแกรมของแต่ละไฟล์เพื่อให้ได้สคริปต์ที่สมบูรณ์ทันที:

### **สคริปต์เชื่อมโยงข้อมูลฐานข้อมูล (database.py)**

"จงเขียนระบบจัดการข้อมูลภาษา Python ชื่อ database.py โดยใช้ไลบรารี psycopg2-binary เชื่อมต่อ PostgreSQL โดยมีฟังก์ชันครบครันในการจัดการตาราง users (ระบบสมัครสมาชิก แฮชรหัสผ่านด้วย bcrypt), user\_settings (บันทึกคีย์ Gemini, Chat ID และพาธรูปเทมเพลตรายบุคคล) และ daily\_reports (การบันทึกรายงานใหม่โดยเก็บ image\_paths เป็นแบบ JSON array, บันทึกการปรับขนาดและประเภทฟอนต์, การดึงประวัติ และฟังก์ชันอัปเดตข้อมูลทับตัวเก่า)"

### **สคริปต์แปลงภาพรายงาน (image\_generator.py)**

"จงสร้างสคริปต์ Python ชื่อ image\_generator.py เพื่อนำข้อมูลจาก Streamlit มาผนวกเข้ากับ HTML Template (templates/report\_template.html) ด้วย Jinja2 เพื่อประมวลตัวแปรฟอนต์และขนาดตัวอักษรของป้ายสไลด์ คอนเวิร์ตรูปภาพกิจกรรมเป็นแบบ Base64 และใช้ playwright (โหมด headless) ถ่ายรูปหน้าสไลด์รายงานออกมาเป็นรูปภาพ PNG ทรงแนวตั้งขนาด 800x1150px ที่มีความละเอียดสมบูรณ์และแสดงสระภาษาไทยได้สวยงาม"